/**
 * Re-exports the entire Firebase layer from @nablis/shared so web-app modules
 * can import from a single short path:
 *
 *   import { db, auth, useAuth, Collections } from '@/lib/firebase'
 */
export * from '@nablis/shared/firebase';
